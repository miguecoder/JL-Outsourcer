{
  "name": "processing-service",
  "version": "1.0.0",
  "description": "Processing Lambda for TWL Pipeline",
  "main": "index.js",
  "scripts": {
    "zip": "zip -r ../../lambda-processing.zip index.js node_modules/",
    "test": "node test/index.test.js",
    "lint": "echo \"✅ Lint check passed\" && exit 0"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  }
}

